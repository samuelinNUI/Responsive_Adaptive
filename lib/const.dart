import 'dart:ui';
import 'package:flex_seed_scheme/flex_seed_scheme.dart';

const Color primarySeedColor = Color(0xFF2C8FFF);
const Color secondarySeedColor = Color(0xFFD1D2D7);
const Color tertiarySeedColor = Color(0xFF5D6273);
const Color errorSeedColor = Color(0xFFFF5449);

// Example definition of light custom tones config.
const FlexTones myLightTones = FlexTones.light(
  backgroundTone: 98,
  surfaceTone: 98,
  paletteType: FlexPaletteType.extended,
);

const mobileWidth = 600;
const tabletWidth = 1100;
const desktopWidth = 1600;